<div class="parentDiv">
    <div style="height:130px;">
      <hr>
    </div>

        <%# new code %>

  <div class="col events-show-card shadow-sm m-1">
      <div class="">
          <%# progress bar %>
          <div class="progress" role="progressbar" aria-label="Success example" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100">
            <div class="progress-bar  text-dark" style="width: <%= (@event.current_amount / @event.goal_amount) * 100 %>%"></div>
          </div>

          <div class="d-flex justify-content-between p-3">
            <div><h6>Current Donation: <%= @event.current_amount.to_i %>€</h6></div>
            <div><h6>Donation Goal: <%= @event.goal_amount.to_i %>€</h6></div>
          </div>
        <%# // progress bar %>


        <%# event title %>
        <div class=" d-flex justify-content-center mt-3">
          <div class="ms-3"><h3><strong>Event:</strong> <%= @event.name%> </h3></div>
        </div>
        <%# //event title %>

        <%# event description: %>
        <div class=" d-flex justify-content-center mt-3">
          <p><strong>Description: </strong> <%= @event.description.capitalize %>.</p>
        </div>
        <%# //event description %>

        <%# event location: %>
        <div class=" d-flex justify-content-center mt-3">
          <p><strong>Location:</strong> <%= @event.address %></p>
        </div>
        <%# //event location %>

        <%# event organiser: %>
        <div class=" d-flex justify-content-center mt-3">
          <p><strong>Event organised by:</strong> <%= @event.user.first_name.capitalize %> <%= @event.user.last_name.capitalize %></p>
        </div>
        <%# //event location %>
        </div>
    </div>

  <div style="width: 100%; height: 250px; margin-bottom:20px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
  <hr>

    <%# donation form %>
      <%= simple_form_for @donation do |f| %>
        <%= f.hidden_field :event_id, value: @event.id %>
        <%= f.input :contribution, label: 'Donate Now!', hint:'(type in desired amount)' %>
        <%= f.submit "Donate Now!", class: "btn btn-sm btn-primary btn-block mt-2" %>
      <% end %>
    <%# join event %>
    <div data-controller="joinbtn">
      <%= simple_form_for [@event, @attendee] do |f| %>
          <% if @existed_attendee %>
            <%= f.submit "Joined", class: "btn btn-sm btn-primary btn-block mt-2 disabled" %>
          <% else %>
            <%= f.submit "Join", class: "btn btn-sm btn-primary btn-block mt-2" %>
          <% end %>
      <% end %>
    </div>

<div>
