<div class="vault-sec text-center ">
  <div class="vault-item ">
    <h2 class="p-3">Vaults</h2>
    <p class="p-5">Vaults are our way of collecting and distributing general donations. Donate to a vault you care about and your contribution will be distributed to those who need.</p>
  </div>

<div class="row p-5">
    <% @vaults.each do |vault| %>
      <div class="col card-vaults shadow-sm m-2" >
      <%= link_to vault_path(vault), class: "text-decoration-none" do %>
          <div >
            <div class="progress" role="progressbar" aria-label="Success example" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100">
            <div class="progress-bar" style="width: <%= (vault.current_amount_vault/vault.donation_amount) * 100 %>%"></div>
          </div>

          <div class="d-flex justify-content-between pt-1">
            <div class="text-start " ><h6 style="color:rgb(90,90,90); margin-top:5px;" >Goal: <%= vault.donation_amount.to_i %>€</h6></div>
            <div class="text-start " > <h6 style="color:rgb(90,90,90);margin-top:5px;" >current donation: <%= vault.current_amount_vault.to_i %>€</h6></div>
          </div>

          <div class="justify-content-center mt-3">
              <% if vault.name == "Housing" %>
              <div class="d-flex justify-content-center mt-5">
                <div>
                    <i class="fa-solid fa-people-roof fa-2xl"></i>
                </div>
                <div>
                    <h3 class="ms-3"><%=  vault.name %></h3>
                </div>

              </div>
                    <p class="text-start p-3"><%= vault.description %></p>

              <% elsif vault.name == "Education" %>
              <div class="d-flex justify-content-center mt-5">
                <div>
                    <i class="fa-solid fa-school fa-2xl"></i>
                                    </div>
                                    <div>
                    <h3 class="ms-3"> <%=  vault.name %> </h3>
            </div>

              </div>

                <p class="text-start p-3"><%= vault.description %></p>

              <% else %>
              <div class="d-flex justify-content-center mt-5">
                <div>
                <i class="fa-solid fa-hand-holding-medical fa-2xl"></i>
                </div>
                <div>
                <h3 class="ms-3"> <%=vault.name %> </h3>
                </div>
              </div>
                <p class="text-start p-3"><%= vault.description %></p>
              <% end %>

              </div>
      <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
